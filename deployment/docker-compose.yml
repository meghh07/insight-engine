version: '3.8'
services:
  insight-engine:
    build: ..
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      ALLOWED_ORIGINS: http://localhost:3000
      AUTH_USERNAME: admin
      AUTH_PASSWORD: change-me
